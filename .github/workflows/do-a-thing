name: do a thing

on:
  push:    

jobs:
  do-a-thing:
    name: do a thing
    runs-on: ubuntu-latest
    steps:
      -
        name: Get docs repository        
        run: |
          git clone https://github.com/pi-hole/docs.git docs-repo
          rm docs-repo/docs/ftldns/configfile.md          
          git diff
