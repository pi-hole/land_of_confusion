name: Test & Build
on:
  release:
    types: [published]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4.2.2
      - name: Run Tests
        run: |
          echo "We are here"
